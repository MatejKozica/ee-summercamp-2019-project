import React from 'react';
import Fragment from 'react';

const Button = (props) => {
    const { children } = props;
    return (
        <div>
            Hello;
        </div>
    )

};

export default Button;